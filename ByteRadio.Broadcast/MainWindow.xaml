<Window x:Class="ByteRadio.Broadcast.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ByteRadio.Broadcast"
        xmlns:converters="clr-namespace:ByteRadio.Broadcast.Converters"
        mc:Ignorable="d"
        Title="ByteRadio Broadcast" Height="360" Width="520"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Window.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ByteRadio Broadcast" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Stream your system audio to a live listener endpoint" Style="{StaticResource SecondaryTextBlockStyle}" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="INGEST WEBSOCKET URL" Style="{StaticResource SecondaryTextBlockStyle}" FontSize="11" Margin="0,0,0,6"/>
            <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Start Broadcast" Width="150" Command="{Binding StartCommand}"/>
            <Button Content="Stop Broadcast" Width="150" Margin="12,0,0,0" Command="{Binding StopCommand}"/>
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="STATUS" Style="{StaticResource SecondaryTextBlockStyle}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding Status}" FontSize="16" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="DATA TRANSFERRED" Style="{StaticResource SecondaryTextBlockStyle}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock FontSize="16" FontWeight="SemiBold">
                        <Run Text="{Binding TransferredMegabytes, StringFormat={}{0:0.00}}"/>
                        <Run Text=" MB"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap"/>
    </Grid>
</Window>
